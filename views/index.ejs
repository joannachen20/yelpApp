<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Yelp Search</title>
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="term" type="text" class="ghost-input" placeholder="Enter a key term" required>
          <input name="location" type="text" class="ghost-input" placeholder="Enter a location" required>
          <input type="submit" class="ghost-button" value="Get Results">
        </form>
        <% if(results !== null){ %>
          <p><%= results %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>

</html>
